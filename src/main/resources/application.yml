external-services:
  google-maps:
    api-key: ${GOOGLE_MAPS_API_KEY}

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG

spring:
  profiles.active: dev
  thymeleaf:
    cache: false
